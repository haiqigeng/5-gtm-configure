interface:
  display_name: "Configure Google Tag Manager"
  short_description: "Complete client-side GTM configuration"
  default_prompt: "Use $configure-gtm to operationally implement my approved analytics tracking plan or explicit media brief across every applicable client-side GTM web-container object in a dedicated workspace as a clean, best-practice, consent-controlled saved setup; preserve analytics requirements exactly, prefer native and supported templates with direct field mappings, safely checkpoint greenfield or delta changes, verify every saved object, return a machine-readable recette handoff, and never publish."
