interface:
  display_name: "Configure Google Tag Manager"
  short_description: "Complete client-side GTM configuration"
  default_prompt: "Use $configure-gtm to operationally implement my approved analytics tracking plan, tracking refonte with client inventory, or explicit media brief across every applicable client-side GTM web-container object in a dedicated workspace as a clean, best-practice, consent-controlled saved setup; preserve analytics requirements exactly, prefer native and supported templates with shape-correct mappings, assign page-view and field ownership, prove fresh pre-change state before delta writes, verify every saved object, finalize the evidence-locked run, return a machine-readable recette handoff, and never publish."
